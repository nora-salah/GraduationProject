import 'package:flutter/material.dart';

class SpecificBlog extends StatelessWidget {
  const SpecificBlog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("SpecificBlog"),
    );
  }
}
